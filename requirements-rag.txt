requests>=2.31
# Full chromadb rather than chromadb-client: the client-only package ships
# without onnxruntime, and the default MiniLM embedder runs client-side.
chromadb>=1.0

# Ingest only. Each is imported inside its loader function, so the API
# container -- which queries but never ingests -- starts without them.
python-docx>=1.1      # .docx -> markdown
pypdf>=4.0            # .pdf  -> markdown (text layer only; scans yield nothing)
beautifulsoup4>=4.12  # .html -> markdown
lxml>=5.0             # bs4's parser
openpyxl>=3.1         # the Excel document manifest
